# --- 解析層(FastAPI) ---
# 起動: cd src/analysis && source .venv/bin/activate && uvicorn app.main:app --port 8001

# --- アプリケーション層(Rails / src/backend) ---
ANALYSIS_SERVICE_URL=http://localhost:8001
FRONTEND_ORIGIN=http://localhost:3000

# --- フロントエンド(Next.js / src/frontend) ---
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
